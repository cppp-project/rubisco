name: 🛠️ Build rubp-build

steps:
  - copy: rubp_build
    to: .bindir/rubp_build
    excludes: ["__pycache__", "*.pyc"]

  - workflow: .rubisco/po.yaml

  - name: 🛠️ Build RuBP.
    type: rubp-pack
