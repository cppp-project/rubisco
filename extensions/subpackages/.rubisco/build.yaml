name: 🛠️ Build subpackages

steps:
  - copy: subpackages
    to: .bindir/subpackages
    excludes: ["__pycache__", "*.pyc"]

  - workflow: .rubisco/po.yaml

  - name: 🛠️ Build RuBP.
    type: rubp-pack
